/*
 * content/ch4.js — Chapter 4 reader content.
 *
 * Text adapted from Structure and Interpretation of Computer Programs, 2e
 * (Abelson & Sussman), Creative Commons BY-SA 4.0 unabridged edition by
 * Andres Raba. Exercises are intentionally omitted per project scope.
 *
 * Block types: see ch1.js header.
 */
window.SICP_CH4 = [
  { t: 'ch', n: '4', title: 'Metalinguistic Abstraction' },
  { t: 'quote', html: '&ldquo;&hellip; It&rsquo;s in words that the magic is&mdash;Abracadabra, Open Sesame, and the rest&mdash;but the magic words in one story aren&rsquo;t magical in the next. The real magic is to understand which words work, and when, and for what; the trick is to learn the trick.<br>&hellip; And those words are made from the letters of our alphabet: a couple-Loss of dozen squiggles we can draw with the pen. This is the key! And the treasure, too, if we can only get our hands on it! It&rsquo;s as if&mdash;as if the key to the treasure <em>is</em> the treasure!&rdquo;<br>&mdash;John Barth, <em>Chimera</em>' },
  { t: 'p', html: 'In our study of program design, we have seen that expert programmers control complexity by building abstractions that hide detail when appropriate. We formulate high-level abstractions by combining elements at a lower level, and we work in terms of these abstractions without attending to the lower-level details. We have used the key tools of procedural abstraction and data abstraction to control complexity. But this is not enough. Often, even the best programming-language abstractions are inadequate to clearly express our ideas. In such cases, we must adopt the technique of <em>metalinguistic abstraction</em>&mdash;establishing new languages.' },
  { t: 'p', html: 'Establishing new languages is a powerful strategy for controlling complexity in engineering design; we can often enhance our ability to deal with a complex problem by adopting a new language that enables us to describe (and hence to think about) the problem in a different way, using primitives, means of combination, and means of abstraction that are particularly well suited to the problem at hand. Programming is endowed with a multitude of languages: machine language, high-level languages, specialized query languages, formatting languages, and so on. Metalinguistic abstraction&mdash;the ability to design and implement new languages&mdash;plays an important role in all branches of engineering.' },
  { t: 'p', html: 'The most fundamental idea in this chapter is that of an <em>evaluator</em> (or <em>interpreter</em>) for a programming language as a procedure that, when applied to an expression of the language, performs the actions required to evaluate that expression. It may seem circular to think of the evaluator, which determines the meaning of expressions in a programming language, as being itself a program. However, evaluation is a process and thus can be described by a program. Furthermore, writing the evaluator as a program gives us a profound vantage point from which to see the deep connection between programs and the data they manipulate, since the evaluator is a program that takes another program as input.' },
  { t: 'p', html: 'The evaluator is just another program, but the significance of this is profound. From this perspective, any evaluator can serve as a description of the language it interprets. The evaluator is a bridge: by writing it, we lay bare the key ideas of the language in a form that is directly executable. Moreover, we gain a powerful instrument for experimenting with language design, since new languages can be explored by modifying or extending the evaluator.' },

  { t: 'sec', n: '4.1', title: 'The Metacircular Evaluator' },
  { t: 'p', html: 'Our evaluator for Lisp will be implemented as a Lisp program. It may seem circular to think about evaluating Lisp programs using an evaluator that is itself implemented in Lisp. However, evaluation is a process, so it is appropriate to describe the evaluation process using Lisp, which, after all, is our tool for describing processes. An evaluator that is written in the same language that it evaluates is said to be <em>metacircular</em>.' },
  { t: 'p', html: 'The metacircular evaluator is essentially a Scheme formulation of the environment model of evaluation described in section 3.2. Recall that the model has two basic parts:' },
  { t: 'ol', items: [
    'To evaluate a combination (a compound expression other than a special form), evaluate the subexpressions and then apply the value of the operator subexpression to the values of the operand subexpressions.',
    'To apply a compound procedure to a set of arguments, evaluate the body of the procedure in a new environment. To construct this environment, extend the environment part of the procedure object by a frame in which the formal parameters of the procedure are bound to the arguments to which the procedure is applied.'
  ] },
  { t: 'p', html: 'These two rules describe the essence of the evaluation process, a basic cycle in which expressions to be evaluated in environments are reduced to procedures to be applied to arguments, which in turn are reduced to new expressions to be evaluated in new environments, and so on, until we get down to symbols, whose values are looked up in the environment, and to primitive procedures, which are applied directly. This evaluation cycle will be embodied by the interplay between the two critical procedures in the evaluator, <code>eval</code> and <code>apply</code>.' },
  { t: 'p', html: 'The implementation of the evaluator will depend upon procedures that define the <em>syntax</em> of the expressions to be evaluated. We will use data abstraction to make the evaluator independent of the representation of the language. For example, rather than committing to a choice that an assignment is to be represented by a list beginning with the symbol <code>set!</code> and that the parts of an assignment are to be accessed by <code>cadr</code> and <code>caddr</code>, we use an abstract predicate <code>assignment?</code> to test for an assignment, and we use abstract selectors <code>assignment-variable</code> and <code>assignment-value</code> to access the parts of an assignment. The implementation of the language representation will be described in detail in section 4.1.2.' },

  { t: 'sub', n: '4.1.1', title: 'The Core of the Evaluator' },
  { t: 'subsub', title: 'Eval' },
  { t: 'p', html: 'The procedure <code>eval</code> takes as arguments an expression and an environment. It classifies the expression and directs its evaluation. <code>Eval</code> is structured as a case analysis of the syntactic type of the expression to be evaluated. In order to keep the procedure general, we express the determination of the type of an expression abstractly, making no commitment to any particular representation for the various types of expressions. Each type of expression has a predicate that tests for it and an abstract means for selecting its parts.' },
  { t: 'code', code: '(define (eval exp env)\n  (cond ((self-evaluating? exp) exp)\n        ((variable? exp) (lookup-variable-value exp env))\n        ((quoted? exp) (text-of-quotation exp))\n        ((assignment? exp) (eval-assignment exp env))\n        ((definition? exp) (eval-definition exp env))\n        ((if? exp) (eval-if exp env))\n        ((lambda? exp)\n         (make-procedure (lambda-parameters exp)\n                         (lambda-body exp)\n                         env))\n        ((begin? exp)\n         (eval-sequence (begin-actions exp) env))\n        ((cond? exp) (eval (cond->if exp) env))\n        ((application? exp)\n         (apply (eval (operator exp) env)\n                (list-of-values (operands exp) env)))\n        (else\n         (error "Unknown expression type: EVAL" exp))))' },
  { t: 'p', html: 'For clarity, <code>eval</code> has been implemented as a case analysis using <code>cond</code>. The disadvantage of this is that our procedure handles only a few distinguishable types of expressions, and no new ones can be defined without editing the definition of <code>eval</code>. In most Lisp implementations, dispatching on the type of an expression is done in a data-directed style. This allows a user to add new types of expressions that <code>eval</code> can distinguish, without modifying the definition of <code>eval</code> itself. (See section 4.1.2.)' },

  { t: 'subsub', title: 'Apply' },
  { t: 'p', html: '<code>Apply</code> takes two arguments: a procedure and a list of arguments to which the procedure should be applied. <code>Apply</code> classifies procedures into two kinds: it calls <code>apply-primitive-procedure</code> to apply primitives; it applies compound procedures by sequentially evaluating the expressions that make up the body of the procedure. The environment for the evaluation of the body of a compound procedure is constructed by extending the base environment carried by the procedure to include a frame that binds the parameters of the procedure to the arguments to which the procedure is to be applied.' },
  { t: 'code', code: '(define (apply procedure arguments)\n  (cond ((primitive-procedure? procedure)\n         (apply-primitive-procedure procedure arguments))\n        ((compound-procedure? procedure)\n         (eval-sequence\n           (procedure-body procedure)\n           (extend-environment\n             (procedure-parameters procedure)\n             arguments\n             (procedure-environment procedure))))\n        (else\n         (error\n          "Unknown procedure type: APPLY" procedure))))' },

  { t: 'subsub', title: 'Procedure arguments' },
  { t: 'p', html: 'When <code>eval</code> processes a procedure application, it uses <code>list-of-values</code> to produce the list of arguments to which the procedure is to be applied. <code>List-of-values</code> takes as an argument the operands of the combination. It evaluates each operand and returns a list of the corresponding values:' },
  { t: 'code', code: '(define (list-of-values exps env)\n  (if (no-operands? exps)\n      \'()\n      (cons (eval (first-operand exps) env)\n            (list-of-values (rest-operands exps) env))))' },

  { t: 'subsub', title: 'Conditionals' },
  { t: 'p', html: '<code>Eval-if</code> evaluates the predicate part of an <code>if</code> expression in the given environment. If the result is true, <code>eval-if</code> evaluates the consequent, otherwise it evaluates the alternative:' },
  { t: 'code', code: '(define (eval-if exp env)\n  (if (true? (eval (if-predicate exp) env))\n      (eval (if-consequent exp) env)\n      (eval (if-alternative exp) env)))' },
  { t: 'p', html: 'The use of <code>true?</code> in <code>eval-if</code> highlights the issue of the connection between an implemented language and an implementation language. The <code>if-predicate</code> is evaluated in the language being implemented and thus yields a value in that language. The interpreter predicate <code>true?</code> translates that value into a value that can be tested by the <code>if</code> in the implementation language: the metacircular representation of truth might not be the same as that of the underlying Scheme.' },

  { t: 'subsub', title: 'Sequences' },
  { t: 'p', html: '<code>Eval-sequence</code> is used by <code>apply</code> to evaluate the sequence of expressions in a procedure body and by <code>eval</code> to evaluate the sequence of expressions in a <code>begin</code> expression. It takes as arguments a sequence of expressions and an environment, and evaluates the expressions in the order in which they occur. The value returned is the value of the final expression.' },
  { t: 'code', code: '(define (eval-sequence exps env)\n  (cond ((last-exp? exps) (eval (first-exp exps) env))\n        (else (eval (first-exp exps) env)\n              (eval-sequence (rest-exps exps) env))))' },

  { t: 'subsub', title: 'Assignments and definitions' },
  { t: 'p', html: 'The following procedure handles assignments to variables. It calls <code>eval</code> to find the value to be assigned and transmits the variable and the resulting value to <code>set-variable-value!</code> to be installed in the designated environment.' },
  { t: 'code', code: '(define (eval-assignment exp env)\n  (set-variable-value! (assignment-variable exp)\n                       (eval (assignment-value exp) env)\n                       env)\n  \'ok)\n\n(define (eval-definition exp env)\n  (define-variable! (definition-variable exp)\n                    (eval (definition-value exp) env)\n                    env)\n  \'ok)' },

  { t: 'sub', n: '4.1.2', title: 'Representing Expressions' },
  { t: 'p', html: 'The evaluator is reminiscent of the symbolic differentiation program discussed in section 2.3.2. Both programs operate on symbolic expressions. In both programs, the result of operating on a compound expression is determined by operating recursively on the pieces of the expression and combining the results in a way that depends on the type of the expression. In both programs we used data abstraction to decouple the general rules of operation from the details of how expressions are represented.' },
  { t: 'p', html: 'The specification of the syntax of the language being evaluated amounts to defining the procedures that extract the pieces of each type of expression and classify expressions by type. The syntax of self-evaluating expressions, variables, quotations, and assignments is straightforward:' },
  { t: 'code', code: '(define (self-evaluating? exp)\n  (cond ((number? exp) true)\n        ((string? exp) true)\n        (else false)))\n\n(define (variable? exp) (symbol? exp))\n\n(define (quoted? exp) (tagged-list? exp \'quote))\n(define (text-of-quotation exp) (cadr exp))\n\n(define (tagged-list? exp tag)\n  (if (pair? exp)\n      (eq? (car exp) tag)\n      false))' },
  { t: 'p', html: 'Assignments have the form <code>(set! &langle;var&rangle; &langle;value&rangle;)</code>:' },
  { t: 'code', code: '(define (assignment? exp) (tagged-list? exp \'set!))\n(define (assignment-variable exp) (cadr exp))\n(define (assignment-value exp) (caddr exp))' },
  { t: 'p', html: 'Definitions have the form <code>(define &langle;var&rangle; &langle;value&rangle;)</code> or the syntactic sugar <code>(define (&langle;var&rangle; &langle;param₁&rangle; &hellip; &langle;paramₙ&rangle;) &langle;body&rangle;)</code>. The latter form is equivalent to <code>(define &langle;var&rangle; (lambda (&langle;param₁&rangle; &hellip; &langle;paramₙ&rangle;) &langle;body&rangle;))</code>.' },
  { t: 'code', code: '(define (definition? exp) (tagged-list? exp \'define))\n(define (definition-variable exp)\n  (if (symbol? (cadr exp))\n      (cadr exp)\n      (caadr exp)))\n(define (definition-value exp)\n  (if (symbol? (cadr exp))\n      (caddr exp)\n      (make-lambda (cdadr exp)   ; formal parameters\n                   (cddr exp)))) ; body' },
  { t: 'p', html: 'Lambda expressions are lists that begin with the symbol <code>lambda</code>:' },
  { t: 'code', code: '(define (lambda? exp) (tagged-list? exp \'lambda))\n(define (lambda-parameters exp) (cadr exp))\n(define (lambda-body exp) (cddr exp))\n\n(define (make-lambda parameters body)\n  (cons \'lambda (cons parameters body)))' },
  { t: 'p', html: 'Conditionals begin with <code>if</code> and have a predicate, a consequent, and an (optional) alternative. If the expression has no alternative part, we provide <code>false</code> as the alternative:' },
  { t: 'code', code: '(define (if? exp) (tagged-list? exp \'if))\n(define (if-predicate exp) (cadr exp))\n(define (if-consequent exp) (caddr exp))\n(define (if-alternative exp)\n  (if (not (null? (cdddr exp)))\n      (cadddr exp)\n      \'false))\n\n(define (make-if predicate consequent alternative)\n  (list \'if predicate consequent alternative))' },
  { t: 'p', html: '<code>Begin</code> packages a sequence of expressions into a single expression:' },
  { t: 'code', code: '(define (begin? exp) (tagged-list? exp \'begin))\n(define (begin-actions exp) (cdr exp))\n(define (last-exp? seq) (null? (cdr seq)))\n(define (first-exp seq) (car seq))\n(define (rest-exps seq) (cdr seq))\n\n(define (sequence->exp seq)\n  (cond ((null? seq) seq)\n        ((last-exp? seq) (first-exp seq))\n        (else (make-begin seq))))\n(define (make-begin seq) (cons \'begin seq))' },
  { t: 'p', html: 'A procedure application is any compound expression that is not one of the above expression types. The <code>car</code> of the expression is the operator, and the <code>cdr</code> is the list of operands:' },
  { t: 'code', code: '(define (application? exp) (pair? exp))\n(define (operator exp) (car exp))\n(define (operands exp) (cdr exp))\n(define (no-operands? ops) (null? ops))\n(define (first-operand ops) (car ops))\n(define (rest-operands ops) (cdr ops))' },

  { t: 'subsub', title: 'Derived expressions' },
  { t: 'p', html: 'Some special forms in our language can be defined in terms of expressions involving other special forms, rather than being implemented directly. One example is <code>cond</code>, which can be implemented as a nest of <code>if</code> expressions. Implementing the evaluation of <code>cond</code> in this way simplifies the evaluator because it reduces the number of special forms for which the evaluation process must be explicitly specified.' },
  { t: 'code', code: '(define (cond? exp) (tagged-list? exp \'cond))\n(define (cond-clauses exp) (cdr exp))\n(define (cond-else-clause? clause)\n  (eq? (cond-predicate clause) \'else))\n(define (cond-predicate clause) (car clause))\n(define (cond-actions clause) (cdr clause))\n(define (cond->if exp)\n  (expand-clauses (cond-clauses exp)))\n(define (expand-clauses clauses)\n  (if (null? clauses)\n      \'false\n      (let ((first (car clauses))\n            (rest (cdr clauses)))\n        (if (cond-else-clause? first)\n            (if (null? rest)\n                (sequence->exp (cond-actions first))\n                (error "ELSE clause isn\'t last: COND->IF"\n                       clauses))\n            (make-if (cond-predicate first)\n                     (sequence->exp (cond-actions first))\n                     (expand-clauses rest))))))' },
  { t: 'p', html: 'Expressions (such as <code>cond</code>) that we choose to implement as syntactic transformations are called <em>derived expressions</em>. <code>Let</code> expressions are also derived expressions.' },

  { t: 'sub', n: '4.1.3', title: 'Evaluator Data Structures' },
  { t: 'p', html: 'In addition to defining the external syntax of expressions, the evaluator implementation must also define the data structures that the evaluator manipulates internally, as part of the execution of a program: the representation of environments and the representation of procedures.' },
  { t: 'subsub', title: 'Testing of predicates' },
  { t: 'code', code: '(define (true? x) (not (eq? x false)))\n(define (false? x) (eq? x false))' },

  { t: 'subsub', title: 'Representing procedures' },
  { t: 'p', html: 'To handle primitives, we assume that we have available the following procedures: <code>(apply-primitive-procedure proc args)</code> applies the given primitive to the argument values, and <code>(primitive-procedure? proc)</code> tests whether <code>proc</code> is a primitive procedure. Compound procedures are constructed from parameters, procedure bodies, and environments:' },
  { t: 'code', code: '(define (make-procedure parameters body env)\n  (list \'procedure parameters body env))\n(define (compound-procedure? p) (tagged-list? p \'procedure))\n(define (procedure-parameters p) (cadr p))\n(define (procedure-body p) (caddr p))\n(define (procedure-environment p) (cadddr p))' },

  { t: 'subsub', title: 'Operations on environments' },
  { t: 'p', html: 'An environment is a sequence of frames, where each frame is a table of bindings. We represent an environment as a list of frames. The enclosing environment of an environment is the <code>cdr</code> of the list. The empty environment is simply the empty list.' },
  { t: 'code', code: '(define (enclosing-environment env) (cdr env))\n(define (first-frame env) (car env))\n(define the-empty-environment \'())\n\n(define (make-frame variables values) (cons variables values))\n(define (frame-variables frame) (car frame))\n(define (frame-values frame) (cdr frame))\n(define (add-binding-to-frame! var val frame)\n  (set-car! frame (cons var (car frame)))\n  (set-cdr! frame (cons val (cdr frame))))\n\n(define (extend-environment vars vals base-env)\n  (if (= (length vars) (length vals))\n      (cons (make-frame vars vals) base-env)\n      (if (< (length vars) (length vals))\n          (error "Too many arguments supplied" vars vals)\n          (error "Too few arguments supplied" vars vals))))' },
  { t: 'p', html: 'To look up a variable in an environment, we scan the first frame. If we find the variable, we return the corresponding value. If not, we search the enclosing environment:' },
  { t: 'code', code: '(define (lookup-variable-value var env)\n  (define (env-loop env)\n    (define (scan vars vals)\n      (cond ((null? vars)\n             (env-loop (enclosing-environment env)))\n            ((eq? var (car vars)) (car vals))\n            (else (scan (cdr vars) (cdr vals)))))\n    (if (eq? env the-empty-environment)\n        (error "Unbound variable" var)\n        (let ((frame (first-frame env)))\n          (scan (frame-variables frame)\n                (frame-values frame)))))\n  (env-loop env))' },

  { t: 'sub', n: '4.1.4', title: 'Running the Evaluator as a Program' },
  { t: 'p', html: 'Given the evaluator, we obtain a working Lisp interpreter by providing it with a primitive procedure mechanism and a driver loop. We set up a global environment that includes bindings for primitive procedures:' },
  { t: 'code', code: '(define (setup-environment)\n  (let ((initial-env\n         (extend-environment (primitive-procedure-names)\n                             (primitive-procedure-objects)\n                             the-empty-environment)))\n    (define-variable! \'true true initial-env)\n    (define-variable! \'false false initial-env)\n    initial-env))\n(define the-global-environment (setup-environment))' },
  { t: 'p', html: 'The driver loop reads an expression, evaluates it in the global environment, and prints the result:' },
  { t: 'code', code: '(define input-prompt ";;; M-Eval input:")\n(define output-prompt ";;; M-Eval value:")\n(define (driver-loop)\n  (prompt-for-input input-prompt)\n  (let ((input (read)))\n    (let ((output (eval input the-global-environment)))\n      (announce-output output-prompt)\n      (user-print output)))\n  (driver-loop))' },
  { t: 'p', html: 'The resulting evaluator can execute any Scheme program, including itself. Running the metacircular evaluator inside itself is a vivid demonstration that the evaluator is &ldquo;just another program&rdquo;: there is nothing magical about the evaluator; it is a systematic case analysis that processes expressions and environments according to the rules of evaluation.' },

  // ── 4.2 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '4.2', title: 'Variations on a Scheme: Lazy Evaluation' },
  { t: 'p', html: 'Now that we have an evaluator expressed as a Lisp program, we can experiment with alternative choices in language design simply by modifying the evaluator. One important variation is <em>lazy evaluation</em> (also called <em>normal-order evaluation</em>), in which the arguments to a procedure are not evaluated until they are actually needed.' },

  { t: 'sub', n: '4.2.1', title: 'Normal Order and Applicative Order' },
  { t: 'p', html: 'In section 1.1.5 we noted that Scheme is an <em>applicative-order</em> language: all the arguments to a procedure are evaluated when the procedure is applied. An alternative evaluation model, <em>normal-order</em> evaluation, would not evaluate the arguments until their values were actually needed. The delayed evaluation of arguments can be a powerful tool. Consider <code>if</code> as an ordinary procedure rather than a special form:' },
  { t: 'code', code: '(define (unless condition usual-value exceptional-value)\n  (if condition exceptional-value usual-value))' },
  { t: 'p', html: 'In applicative order, calling <code>(unless (= b 0) (/ a b) (begin (display "zero") 0))</code> would evaluate <code>(/ a b)</code> even when <code>b</code> is 0, causing a division-by-zero error. With lazy evaluation, <code>(/ a b)</code> would only be evaluated if <code>b</code> is not 0.' },
  { t: 'p', html: 'Lazy evaluation is not without costs. It makes the order of evaluation unpredictable, which interacts badly with side effects. A language designed around lazy evaluation (like Haskell) typically forbids side effects, enforcing a purely functional style. Our lazy evaluator allows us to explore these ideas within a Scheme-like framework.' },

  { t: 'sub', n: '4.2.2', title: 'An Interpreter with Lazy Evaluation' },
  { t: 'p', html: 'The basic idea is to defer evaluation of procedure arguments by creating <em>thunks</em>. A thunk is a &ldquo;promise&rdquo; to evaluate an expression in an environment. When the value is needed (for example, as the operator position of a combination, as the test in a conditional, or by a primitive procedure), the thunk is <em>forced</em>: the expression is evaluated and the result is cached.' },
  { t: 'p', html: 'The main change to the evaluator is in <code>apply</code>. Instead of evaluating the operands before applying the procedure, we delay them:' },
  { t: 'code', code: '(define (apply procedure arguments env)\n  (cond ((primitive-procedure? procedure)\n         (apply-primitive-procedure\n          procedure\n          (list-of-arg-values arguments env))) ; force for primitives\n        ((compound-procedure? procedure)\n         (eval-sequence\n          (procedure-body procedure)\n          (extend-environment\n           (procedure-parameters procedure)\n           (list-of-delayed-args arguments env) ; delay for compound\n           (procedure-environment procedure))))\n        (else (error "Unknown procedure type" procedure))))\n\n(define (list-of-arg-values exps env)\n  (if (no-operands? exps) \'()\n      (cons (actual-value (first-operand exps) env)\n            (list-of-arg-values (rest-operands exps) env))))\n\n(define (list-of-delayed-args exps env)\n  (if (no-operands? exps) \'()\n      (cons (delay-it (first-operand exps) env)\n            (list-of-delayed-args (rest-operands exps) env))))' },
  { t: 'p', html: 'Thunks are represented as tagged lists. <code>Force-it</code> evaluates a thunk and caches the result (memoization), so subsequent forces return immediately:' },
  { t: 'code', code: '(define (delay-it exp env) (list \'thunk exp env))\n(define (thunk? obj) (tagged-list? obj \'thunk))\n(define (thunk-exp thunk) (cadr thunk))\n(define (thunk-env thunk) (caddr thunk))\n\n(define (evaluated-thunk? obj) (tagged-list? obj \'evaluated-thunk))\n(define (thunk-value evaluated-thunk) (cadr evaluated-thunk))\n\n(define (force-it obj)\n  (cond ((thunk? obj)\n         (let ((result (actual-value (thunk-exp obj) (thunk-env obj))))\n           (set-car! obj \'evaluated-thunk)\n           (set-car! (cdr obj) result)\n           (set-cdr! (cdr obj) \'())\n           result))\n        ((evaluated-thunk? obj) (thunk-value obj))\n        (else obj)))' },

  { t: 'sub', n: '4.2.3', title: 'Streams as Lazy Lists' },
  { t: 'p', html: 'With lazy evaluation, streams no longer need to be a special data structure. We can simply use ordinary lists, and the laziness of the evaluator will ensure that list elements are computed only when needed. In the lazy evaluator, <code>cons</code> delays both its arguments (or at least the second one), so <code>(cons a b)</code> does not evaluate <code>b</code> until it is accessed. This means every <code>cons</code> is automatically a <code>cons-stream</code>.' },
  { t: 'p', html: 'This is elegant: the stream abstraction from section 3.5 was a workaround for applicative-order evaluation. In a lazy language, that workaround disappears. Lists and streams are the same thing. This unification is one of the advantages of lazy evaluation as a language design choice.' },

  // ── 4.3 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '4.3', title: 'Variations on a Scheme: Nondeterministic Computing' },
  { t: 'p', html: 'In this section, we extend the Scheme evaluator to support a style of programming called <em>nondeterministic computing</em>, which is useful for solving combinatorial search problems. The idea is to provide two special forms: <code>amb</code> and automatic backtracking.' },

  { t: 'sub', n: '4.3.1', title: 'Amb and Search' },
  { t: 'p', html: 'The expression <code>(amb e₁ e₂ &hellip; eₙ)</code> returns the value of one of the expressions eᵢ &ldquo;ambiguously.&rdquo; The choice is made in such a way that the overall computation succeeds. If the current choice leads to a failure (signaled by <code>(amb)</code> with no arguments), the evaluator backtracks and tries the next alternative. For example:' },
  { t: 'code', code: '(define (require p) (if (not p) (amb)))\n\n(define (an-element-of items)\n  (require (not (null? items)))\n  (amb (car items) (an-element-of (cdr items))))\n\n(define (an-integer-starting-from n)\n  (amb n (an-integer-starting-from (+ n 1))))' },
  { t: 'p', html: 'Using <code>amb</code>, we can express logic puzzles very naturally. The following finds all Pythagorean triples:' },
  { t: 'code', code: '(define (a-pythagorean-triple-between low high)\n  (let ((i (an-integer-between low high)))\n    (let ((j (an-integer-between i high)))\n      (let ((k (an-integer-between j high)))\n        (require (= (+ (* i i) (* j j)) (* k k)))\n        (list i j k)))))\n\n(define (an-integer-between low high)\n  (require (<= low high))\n  (amb low (an-integer-between (+ low 1) high)))' },
  { t: 'p', html: 'The beauty of this approach is that we write the <em>specification</em> of what we want (a triple where i&sup2; + j&sup2; = k&sup2;), and the evaluator&rsquo;s built-in search mechanism finds it. We don&rsquo;t need to write a search algorithm.' },

  { t: 'sub', n: '4.3.2', title: 'Examples of Nondeterministic Programs' },
  { t: 'p', html: 'Nondeterministic programming is particularly well-suited for natural-language parsing. Consider a simple grammar: a sentence is a noun phrase followed by a verb phrase; a noun phrase is an article followed by a noun; a verb phrase is a verb, or a verb followed by a noun phrase. We can write a parser that uses <code>amb</code> to try all possible parsings:' },
  { t: 'code', code: '(define nouns \'(noun student professor cat class))\n(define verbs \'(verb studies lectures eats sleeps))\n(define articles \'(article the a))\n(define prepositions \'(prep for to in by with))\n\n(define (parse-sentence)\n  (list \'sentence (parse-noun-phrase) (parse-verb-phrase)))\n(define (parse-noun-phrase)\n  (list \'noun-phrase (parse-word articles) (parse-word nouns)))\n(define (parse-verb-phrase)\n  (define (maybe-extend verb-phrase)\n    (amb verb-phrase\n         (maybe-extend\n          (list \'verb-phrase verb-phrase\n                (parse-prepositional-phrase)))))\n  (maybe-extend (parse-word verbs)))\n(define (parse-prepositional-phrase)\n  (list \'prep-phrase (parse-word prepositions)\n                     (parse-noun-phrase)))' },
  { t: 'p', html: 'This parser is remarkably concise. The grammar is expressed directly in the code; <code>amb</code> and backtracking handle the search over possible parse trees automatically.' },

  { t: 'sub', n: '4.3.3', title: 'Implementing the Amb Evaluator' },
  { t: 'p', html: 'The <code>amb</code> evaluator is implemented by modifying the basic evaluator to use <em>continuation-passing style</em>. Instead of returning a value, <code>eval</code> takes two additional arguments: a <em>success continuation</em> and a <em>failure continuation</em>. The success continuation is a procedure to call with the computed value; the failure continuation is a procedure to call if the computation fails (to trigger backtracking).' },
  { t: 'code', code: '(define (ambeval exp env succeed fail)\n  (cond ((self-evaluating? exp) (succeed exp fail))\n        ((variable? exp)\n         (succeed (lookup-variable-value exp env) fail))\n        ((amb? exp)\n         (analyze-amb exp env succeed fail))\n        ;; ... other expression types ...\n        ))' },
  { t: 'p', html: 'The key is the <code>amb</code> handler. It tries each alternative in turn, passing a failure continuation that tries the next alternative:' },
  { t: 'code', code: '(define (analyze-amb exp)\n  (let ((cprocs (map analyze (amb-choices exp))))\n    (lambda (env succeed fail)\n      (define (try-next choices)\n        (if (null? choices)\n            (fail)\n            ((car choices) env\n                           succeed\n                           (lambda () (try-next (cdr choices))))))\n      (try-next cprocs))))' },
  { t: 'p', html: 'This is a powerful technique. By threading continuations through the evaluation, we get backtracking search &ldquo;for free&rdquo;: each <code>amb</code> point remembers how to try its next alternative, and failure cascades back through the chain of continuations until an untried alternative is found.' },

  // ── 4.4 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '4.4', title: 'Logic Programming' },
  { t: 'p', html: 'In this chapter we have extended our language with assignment (<code>set!</code>), with lazy evaluation, and with nondeterministic choice (<code>amb</code>). Now we take a more radical step: we replace the entire procedural paradigm with a <em>declarative</em> one. Instead of describing how to compute an answer, we describe what relationships hold, and let the system figure out how to derive answers. This is the idea behind <em>logic programming</em>.' },

  { t: 'sub', n: '4.4.1', title: 'Deductive Information Retrieval' },
  { t: 'p', html: 'We implement a <em>query language</em> that allows us to retrieve and reason about information stored in a database of facts. The database contains assertions such as:' },
  { t: 'code', code: '(address (Bitdiddle Ben) (Slumerville (Ridge Road) 10))\n(job (Bitdiddle Ben) (computer wizard))\n(salary (Bitdiddle Ben) 60000)\n(supervisor (Bitdiddle Ben) (Warbucks Oliver))' },
  { t: 'p', html: 'We can retrieve information using <em>simple queries</em> with pattern variables (denoted by <code>?</code>):' },
  { t: 'code', code: ';;; Query input:\n(job ?x (computer wizard))\n;;; Query results:\n(job (Bitdiddle Ben) (computer wizard))' },
  { t: 'p', html: 'Queries can be combined with logical operators. <em>Compound queries</em> use <code>and</code>, <code>or</code>, and <code>not</code>:' },
  { t: 'code', code: '(and (job ?person (computer . ?type))\n     (address ?person ?where))\n\n(and (salary ?person ?amount)\n     (lisp-value > ?amount 50000))' },
  { t: 'p', html: 'We can also define <em>rules</em>, which are logical implications. A rule states that a conclusion holds if certain conditions are satisfied:' },
  { t: 'code', code: '(rule (lives-near ?person-1 ?person-2)\n      (and (address ?person-1 (?town . ?rest-1))\n           (address ?person-2 (?town . ?rest-2))\n           (not (same ?person-1 ?person-2))))\n\n(rule (same ?x ?x))' },
  { t: 'p', html: 'Rules can be recursive. For instance, the <code>outranked-by</code> relation:' },
  { t: 'code', code: '(rule (outranked-by ?staff-person ?boss)\n      (or (supervisor ?staff-person ?boss)\n          (and (supervisor ?staff-person ?middle-manager)\n               (outranked-by ?middle-manager ?boss))))' },

  { t: 'sub', n: '4.4.2', title: 'How the Query System Works' },
  { t: 'p', html: 'The query system is organized around two central operations: <em>pattern matching</em> and <em>unification</em>. Pattern matching is used for simple queries: given a pattern and a datum, the matcher determines whether the datum matches the pattern and, if so, produces a binding of pattern variables to subexpressions of the datum.' },
  { t: 'p', html: 'Unification is a generalization of pattern matching used for applying rules. In unification, both the &ldquo;pattern&rdquo; and the &ldquo;datum&rdquo; may contain variables. The unifier finds an assignment of values to variables that makes the two expressions equal, if such an assignment exists. For example, unifying <code>(?x a ?y)</code> with <code>(?y ?z a)</code> yields <code>?x = a</code>, <code>?y = a</code>, <code>?z = a</code>.' },
  { t: 'p', html: 'To process a query, the system maintains a stream of <em>frames</em>, where each frame represents a set of variable bindings. A simple query filters the stream by pattern-matching against the database. A compound query (<code>and</code>, <code>or</code>, <code>not</code>) combines the results of subqueries. A rule application uses unification to match the rule&rsquo;s conclusion against the query, then processes the rule&rsquo;s body with the resulting bindings.' },

  { t: 'sub', n: '4.4.3', title: 'Is Logic Programming Mathematical Logic?' },
  { t: 'p', html: 'The query language may resemble mathematical logic, but there are important differences. The <code>not</code> in our query language is not logical negation; it is &ldquo;not deducible from the current database.&rdquo; This is the <em>closed-world assumption</em>: anything not in the database or derivable from it is considered false.' },
  { t: 'p', html: 'Similarly, the query language&rsquo;s <code>and</code> and <code>or</code> are not the commutative operations of logic. The order of clauses in an <code>and</code> query can affect whether the computation terminates, because the system processes them left to right. Infinite loops can arise from recursive rules if the system is not careful about the order of evaluation.' },
  { t: 'p', html: 'These differences reflect a fundamental tension: logic describes <em>what</em> is true, but computation must specify <em>how</em> to derive truths. The query language is a compromise; it uses logic-like notation but employs a specific computational strategy (depth-first search with backtracking). The relationship between logic and computation is one of the deepest topics in computer science, and the field of logic programming continues to explore it.' },

  { t: 'sub', n: '4.4.4', title: 'Implementing the Query System' },
  { t: 'p', html: 'The query evaluator is organized as a collection of procedures that process queries against streams of frames. The central procedure <code>qeval</code> dispatches on the type of query:' },
  { t: 'code', code: '(define (qeval query frame-stream)\n  (let ((qproc (get (type query) \'qeval)))\n    (if qproc\n        (qproc (contents query) frame-stream)\n        (simple-query query frame-stream))))' },
  { t: 'p', html: 'Simple queries are processed by scanning the database for matching assertions and by applying matching rules:' },
  { t: 'code', code: '(define (simple-query query-pattern frame-stream)\n  (stream-flatmap\n   (lambda (frame)\n     (stream-append-delayed\n      (find-assertions query-pattern frame)\n      (delay (apply-rules query-pattern frame))))\n   frame-stream))' },
  { t: 'p', html: 'The use of <code>delay</code> in rule application prevents infinite loops from blocking the entire computation. Compound queries are handled by dedicated procedures: <code>conjoin</code> for <code>and</code> (filters the frame stream through each conjunct in turn), <code>disjoin</code> for <code>or</code> (merges the frame streams from each disjunct), and <code>negate</code> for <code>not</code> (filters out frames for which the negated query succeeds).' },
  { t: 'p', html: 'The unifier is the heart of rule application. It extends pattern matching to handle variables on both sides of the comparison, building up a frame of bindings that makes two patterns identical:' },
  { t: 'code', code: '(define (unify-match p1 p2 frame)\n  (cond ((eq? frame \'failed) \'failed)\n        ((equal? p1 p2) frame)\n        ((var? p1) (extend-if-consistent p1 p2 frame))\n        ((var? p2) (extend-if-consistent p2 p1 frame))\n        ((and (pair? p1) (pair? p2))\n         (unify-match (cdr p1) (cdr p2)\n                      (unify-match (car p1) (car p2) frame)))\n        (else \'failed)))' },
  { t: 'p', html: 'The query system brings together many of the ideas from this chapter: data-directed dispatch (section 2.4), streams (section 3.5), and environment management. It demonstrates that by changing the evaluator, we can create languages with radically different computational paradigms.' },
];
