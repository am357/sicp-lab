/*
 * content/ch3.js — Chapter 3 reader content.
 *
 * Text adapted from Structure and Interpretation of Computer Programs, 2e
 * (Abelson & Sussman), Creative Commons BY-SA 4.0 unabridged edition by
 * Andres Raba. Exercises are intentionally omitted per project scope.
 *
 * Block types: see ch1.js header.
 */
window.SICP_CH3 = [
  { t: 'sec', n: '3.1', title: 'Assignment and Local State' },
  { t: 'p', html: 'We ordinarily view the world as populated by independent objects, each of which has a state that changes over time. An object is said to &ldquo;have state&rdquo; if its behavior is influenced by its history. A bank account, for example, has state in that the answer to the question &ldquo;Can I withdraw $100?&rdquo; depends upon the history of deposit and withdrawal transactions. We can characterize an object&rsquo;s state by one or more <em>state variables</em>, which among them maintain enough information about history to determine the object&rsquo;s current behavior.' },
  { t: 'p', html: 'In a system composed of many objects, the objects are rarely completely independent. Each may influence the states of others through interactions, which serve to couple the state variables of one object to those of other objects. Indeed, the view that a system is composed of separate objects is most useful when the state variables of the system can be grouped into closely coupled subsystems that are only loosely coupled to other subsystems.' },
  { t: 'p', html: 'This view of a system can be a powerful framework for organizing computational models of the system. For such a model to be modular, it should be decomposed into computational objects that model the actual objects in the system. Each computational object must have its own <em>local state variables</em> describing the actual object&rsquo;s state. Since the states of objects in the system being modeled change over time, the state variables of the corresponding computational objects must also change. If we choose to model the flow of time in the system by the elapsed time in the computer, then we must have a way to construct computational objects whose behaviors change as our programs run. In particular, if we wish to model state variables by ordinary symbolic names in the programming language, then the language must provide an <em>assignment operator</em> to enable us to change the value associated with a name.' },

  { t: 'sub', n: '3.1.1', title: 'Local State Variables' },
  { t: 'p', html: 'To illustrate what we mean by having a computational object with time-varying state, let us model the situation of withdrawing money from a bank account. We will do this using a procedure <code>withdraw</code>, which takes as argument an <code>amount</code> to be withdrawn. If there is enough money in the account to accommodate the withdrawal, then <code>withdraw</code> should return the balance remaining after the withdrawal. Otherwise, <code>withdraw</code> should return the message <em>Insufficient funds</em>. For example, if we begin with $100 in the account, we should obtain the following sequence of responses using <code>withdraw</code>:' },
  { t: 'code', code: '(withdraw 25)   ; 75\n(withdraw 25)   ; 50\n(withdraw 60)   ; "Insufficient funds"\n(withdraw 15)   ; 35' },
  { t: 'p', html: 'Observe that the expression <code>(withdraw 25)</code>, evaluated twice, yields different values. This is a new kind of behavior for a procedure. Until now, all our procedures could be viewed as specifications for computing mathematical functions. A call to a procedure computed the value of the function applied to the given arguments, and two calls to the same procedure with the same arguments always produced the same result.' },
  { t: 'p', html: 'To implement <code>withdraw</code>, we can use a variable <code>balance</code> to indicate the balance of money in the account and define <code>withdraw</code> as a procedure that accesses <code>balance</code>. The <code>withdraw</code> procedure checks to see if <code>balance</code> is at least as large as the requested <code>amount</code>. If so, <code>withdraw</code> decrements <code>balance</code> by <code>amount</code> and returns the new value of <code>balance</code>. Otherwise, <code>withdraw</code> returns the <em>Insufficient funds</em> message. Here are the definitions of <code>balance</code> and <code>withdraw</code>:' },
  { t: 'code', live: true, code: '(define balance 100)\n(define (withdraw amount)\n  (if (>= balance amount)\n      (begin (set! balance (- balance amount))\n             balance)\n      "Insufficient funds"))\n(withdraw 25)\n(withdraw 25)\n(withdraw 60)\n(withdraw 15)' },
  { t: 'p', html: 'Decrementing <code>balance</code> is accomplished by the expression <code>(set! balance (- balance amount))</code>. This uses the <code>set!</code> special form, whose syntax is <code>(set! &langle;name&rangle; &langle;new-value&rangle;)</code>. Here <code>&langle;name&rangle;</code> is a symbol and <code>&langle;new-value&rangle;</code> is any expression. <code>Set!</code> changes <code>&langle;name&rangle;</code> so that its value is the result obtained by evaluating <code>&langle;new-value&rangle;</code>.' },
  { t: 'p', html: 'The expression <code>(begin exp₁ exp₂ &hellip; expₖ)</code> causes the expressions exp₁ through expₖ to be evaluated in sequence and the value of the final expression expₖ to be returned as the value of the entire <code>begin</code> form.' },
  { t: 'p', html: 'Although <code>withdraw</code> works as desired, the variable <code>balance</code> presents a problem. As specified above, <code>balance</code> is a name defined in the global environment and is freely accessible to be examined or modified by any procedure. It would be much better if we could somehow make <code>balance</code> internal to <code>withdraw</code>, so that <code>withdraw</code> would be the only procedure that could access <code>balance</code> directly and any other procedure could access <code>balance</code> only indirectly (through calls to <code>withdraw</code>). This would more accurately model the notion that <code>balance</code> is a local state variable used by <code>withdraw</code> to keep track of the state of the account.' },
  { t: 'p', html: 'We can make <code>balance</code> internal to <code>withdraw</code> by rewriting the definition as follows:' },
  { t: 'code', live: true, code: '(define new-withdraw\n  (let ((balance 100))\n    (lambda (amount)\n      (if (>= balance amount)\n          (begin (set! balance (- balance amount))\n                 balance)\n          "Insufficient funds"))))\n(new-withdraw 25)\n(new-withdraw 25)\n(new-withdraw 60)\n(new-withdraw 15)' },
  { t: 'p', html: 'What we have done here is use <code>let</code> to establish an environment with a local variable <code>balance</code>, bound to the initial value 100. Within this local environment, we use <code>lambda</code> to create a procedure that takes <code>amount</code> as an argument and behaves like our previous <code>withdraw</code> procedure. This procedure, returned as the result of evaluating the <code>let</code> expression, behaves in precisely the same way as <code>withdraw</code>, but its variable <code>balance</code> is not accessible by any other procedure.' },
  { t: 'p', html: 'Combining <code>set!</code> with local variables is the general programming technique we will use for constructing computational objects with local state. Unfortunately, using this technique raises a serious problem: when we first introduced procedures, we also introduced the substitution model of evaluation (section 1.1.5) to provide an interpretation of what procedure application means. We said that applying a procedure should be interpreted as evaluating the body of the procedure with the formal parameters replaced by their values. The trouble is that, as soon as we introduce assignment into our language, substitution is no longer an adequate model of procedure application.' },
  { t: 'p', html: 'As long as we do not use assignments, two evaluations of the same procedure with the same arguments will produce the same result, so that procedures can be viewed as computing mathematical functions. Programming without any use of assignments, as we did throughout the first two chapters of this book, is accordingly known as <em>functional programming</em>.' },
  { t: 'p', html: 'To understand how assignment complicates matters, consider a simplified version of the <code>make-withdraw</code> procedure from above that does not bother to check for an insufficient amount:' },
  { t: 'code', live: true, code: '(define (make-decrementer balance)\n  (lambda (amount) (- balance amount)))\n\n(define d (make-decrementer 25))\n(d 20)\n(d 10)' },
  { t: 'p', html: '<code>make-decrementer</code> returns the same result each time it is called with the same argument. Now compare this with <code>make-simplified-withdraw</code>:' },
  { t: 'code', live: true, code: '(define (make-simplified-withdraw balance)\n  (lambda (amount)\n    (set! balance (- balance amount))\n    balance))\n\n(define w (make-simplified-withdraw 25))\n(w 20)\n(w 10)' },
  { t: 'p', html: 'Here, successive calls to <code>w</code> yield different results. The substitution model cannot explain this. We need a more refined model: the <em>environment model</em> of evaluation, which we develop in section 3.2.' },

  { t: 'subsub', title: 'Message passing and local state' },
  { t: 'p', html: 'We can make a more complete financial object by letting the &ldquo;account&rdquo; itself be a procedure that receives a request and dispatches it:' },
  { t: 'code', live: true, code: '(define (make-account balance)\n  (define (withdraw amount)\n    (if (>= balance amount)\n        (begin (set! balance (- balance amount))\n               balance)\n        "Insufficient funds"))\n  (define (deposit amount)\n    (set! balance (+ balance amount))\n    balance)\n  (define (dispatch m)\n    (cond ((eq? m \'withdraw) withdraw)\n          ((eq? m \'deposit) deposit)\n          (else (error "Unknown request: MAKE-ACCOUNT" m))))\n  dispatch)\n\n(define acc (make-account 100))\n((acc \'withdraw) 50)\n((acc \'withdraw) 60)\n((acc \'deposit) 40)\n((acc \'withdraw) 60)' },
  { t: 'p', html: 'Each call to <code>make-account</code> sets up an environment with a local state variable <code>balance</code>. Within this environment, <code>make-account</code> defines procedures <code>deposit</code> and <code>withdraw</code> that access <code>balance</code> and an additional procedure <code>dispatch</code> that takes a &ldquo;message&rdquo; as input and returns one of the two local procedures. The <code>dispatch</code> procedure itself is returned as the value that represents the bank-account object. This is precisely the <em>message-passing</em> style of programming that we saw in section 2.1.3, although here we use it in conjunction with the ability to modify local variables.' },

  { t: 'sub', n: '3.1.2', title: 'The Benefits of Introducing Assignment' },
  { t: 'p', html: 'As we shall see, introducing assignment into our programming language leads us into a thicket of difficult conceptual issues. Nevertheless, viewing systems as collections of objects with local state is a powerful technique for maintaining a modular design. As a simple example, consider the design of a procedure <code>rand</code> that, whenever it is called, returns a nonnegative integer chosen at random.' },
  { t: 'p', html: 'It is not at all clear what is meant by &ldquo;chosen at random.&rdquo; What we presumably want is for successive calls to <code>rand</code> to produce a sequence of numbers that has statistical properties of uniform distribution. We will not discuss methods for generating suitable sequences here. Rather, let us assume that we have a procedure <code>rand-update</code> that has the property that if we start with a given number x₁ and form x₂ = (rand-update x₁), x₃ = (rand-update x₂), then the sequence of values x₁, x₂, x₃, &hellip; will have the desired statistical properties.' },
  { t: 'p', html: 'We can implement <code>rand</code> as a procedure with a local state variable <code>x</code> that is initialized to some fixed value <code>random-init</code>. Each call to <code>rand</code> computes <code>rand-update</code> of the current value of <code>x</code>, stores this as the new value of <code>x</code>, and returns the result:' },
  { t: 'code', live: true, code: '(define random-init 42)\n(define (rand-update x)\n  (remainder (+ (* 1103515245 x) 12345) 2147483648))\n\n(define rand\n  (let ((x random-init))\n    (lambda ()\n      (set! x (rand-update x))\n      x)))\n\n(rand)\n(rand)\n(rand)' },
  { t: 'p', html: 'Of course, we could generate the same sequence of random numbers without using assignment by simply calling <code>rand-update</code> directly. However, this would mean that any part of our program that used random numbers would have to explicitly remember the current value of <code>x</code> to be passed as an argument to <code>rand-update</code>. The use of assignment and the technique of hiding state in local variables eliminates this bookkeeping and allows us to think about random-number generation in a modular way.' },

  { t: 'sub', n: '3.1.3', title: 'The Costs of Introducing Assignment' },
  { t: 'p', html: 'As we have seen, the <code>set!</code> operation enables us to model objects that have local state. However, this advantage comes at a price. Our programming language can no longer be interpreted in terms of the substitution model of procedure application that we introduced in section 1.1.5. Moreover, no simple model with &ldquo;nice&rdquo; mathematical properties can be an adequate framework for dealing with objects and assignment in a programming language.' },
  { t: 'p', html: 'So long as we do not use assignments, two evaluations of the same procedure with the same arguments will produce the same result, so that procedures can be viewed as computing mathematical functions. Programming without any use of assignments, as we did throughout the first two chapters of this book, is accordingly known as <em>functional programming</em>.' },
  { t: 'p', html: 'To understand how assignment complicates matters, consider the <code>make-decrementer</code> procedure that does not use <code>set!</code>:' },
  { t: 'code', code: '(define (make-decrementer balance)\n  (lambda (amount) (- balance amount)))' },
  { t: 'p', html: 'Using the substitution model, <code>((make-decrementer 25) 20)</code> reduces as follows:' },
  { t: 'code', code: '((make-decrementer 25) 20)\n((lambda (amount) (- 25 amount)) 20)\n(- 25 20)\n5' },
  { t: 'p', html: 'Now consider <code>make-simplified-withdraw</code>, which uses <code>set!</code>:' },
  { t: 'code', code: '(define (make-simplified-withdraw balance)\n  (lambda (amount)\n    (set! balance (- balance amount))\n    balance))' },
  { t: 'p', html: 'If we attempt to apply the substitution model to <code>((make-simplified-withdraw 25) 20)</code>, we would substitute 25 for <code>balance</code> in the body of the procedure to get <code>(lambda (amount) (set! 25 (- 25 amount)) 25)</code>. This makes no sense: we cannot set a number to a new value. The trouble here is that substitution is ultimately founded on the notion that the symbols in our language are essentially names for values. But as soon as we introduce <code>set!</code> and the idea that the value of a variable can change, a variable can no longer be simply a name. Now a variable somehow refers to a place where a value can be stored, and the value stored at this place can change.' },

  { t: 'subsub', title: 'Sameness and change' },
  { t: 'p', html: 'The issue surfacing here is more profound than the mere breakdown of a particular model of computation. As soon as we introduce change into our computational models, many notions that were previously straightforward become problematical. Consider the concept of two things being &ldquo;the same.&rdquo;' },
  { t: 'p', html: 'Suppose we call <code>make-decrementer</code> twice with the same argument to create two procedures:' },
  { t: 'code', code: '(define d1 (make-decrementer 25))\n(define d2 (make-decrementer 25))' },
  { t: 'p', html: 'Are <code>d1</code> and <code>d2</code> the same? An acceptable answer is yes, because <code>d1</code> and <code>d2</code> have the same computational behavior: each is a procedure that subtracts its input from 25. In fact, <code>d1</code> could be substituted for <code>d2</code> in any computation without changing the result. Contrast this with making two calls to <code>make-simplified-withdraw</code>:' },
  { t: 'code', live: true, code: '(define (make-simplified-withdraw balance)\n  (lambda (amount)\n    (set! balance (- balance amount))\n    balance))\n\n(define w1 (make-simplified-withdraw 25))\n(define w2 (make-simplified-withdraw 25))\n(w1 20)\n(w1 10)\n(w2 20)' },
  { t: 'p', html: 'Are <code>w1</code> and <code>w2</code> the same? Surely not, because calls to <code>w1</code> and <code>w2</code> have distinct effects: <code>w1</code> has been decremented twice, while <code>w2</code> only once. Even though <code>w1</code> and <code>w2</code> are &ldquo;equal&rdquo; in the sense that they are both created by evaluating the same expression, it is not true that <code>w1</code> could be substituted for <code>w2</code> in any expression without changing the result.' },
  { t: 'p', html: 'A language that supports the concept that &ldquo;equals can be substituted for equals&rdquo; in an expression without changing the value of the expression is said to be <em>referentially transparent</em>. Referential transparency is violated when we include <code>set!</code> in our computer language. This makes it tricky to determine when we can simplify expressions by substituting equivalent expressions. Consequently, reasoning about programs that use assignment becomes drastically more difficult.' },

  { t: 'subsub', title: 'Pitfalls of imperative programming' },
  { t: 'p', html: 'In contrast to functional programming, programming that makes extensive use of assignment is known as <em>imperative programming</em>. In addition to raising complications about computational models, programs written in imperative style are susceptible to bugs that cannot occur in functional programs. For example, recall the iterative factorial program from section 1.2.1:' },
  { t: 'code', code: '(define (factorial n)\n  (define (iter product counter)\n    (if (> counter n)\n        product\n        (iter (* counter product)\n              (+ counter 1))))\n  (iter 1 1))' },
  { t: 'p', html: 'Instead of passing arguments in the internal iterative loop, we could adopt a more imperative style by using explicit assignment to update the values of the variables <code>product</code> and <code>counter</code>:' },
  { t: 'code', live: true, code: '(define (factorial n)\n  (let ((product 1)\n        (counter 1))\n    (define (iter)\n      (if (> counter n)\n          product\n          (begin (set! product (* counter product))\n                 (set! counter (+ counter 1))\n                 (iter))))\n    (iter)))\n(factorial 5)' },
  { t: 'p', html: 'This does not change the results produced by the program, but it does introduce a subtle trap. How do we decide the order of the assignments? As it happens, the program is correct as written. But writing the assignments in the opposite order, <code>(set! counter (+ counter 1))</code> before <code>(set! product (* counter product))</code>, would have produced a different, incorrect result. In general, programming with assignment forces us to carefully consider the orders of the assignments to make sure that each statement uses the correct version of the variables that have been changed. This issue simply does not arise in functional programs. The complexity of imperative programs becomes even worse if we consider applications in which several processes may be executing concurrently.' },

  // ── 3.2 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '3.2', title: 'The Environment Model of Evaluation' },
  { t: 'p', html: 'When we introduced compound procedures in chapter 1, we used the substitution model of evaluation (section 1.1.5) to define what is meant by applying a procedure to arguments: to apply a compound procedure to arguments, evaluate the body of the procedure with each formal parameter replaced by the corresponding argument. The trouble with this model is that it fails once we allow assignment in our language.' },
  { t: 'p', html: 'In our new model of evaluation, a variable no longer refers directly to a value. Instead, a variable refers to a place (or <em>binding</em>) in an <em>environment</em>. An environment is a sequence of <em>frames</em>. Each frame is a table (possibly empty) of <em>bindings</em>, which associate variable names with their corresponding values. (A single frame may contain at most one binding for any variable.) Each frame also has a pointer to its <em>enclosing environment</em>, unless, for the purposes of discussion, the frame is considered to be <em>global</em>. The value of a variable with respect to an environment is the value given by the binding of the variable in the first frame in the environment that contains a binding for that variable. If no frame in the sequence specifies a binding for the variable, then the variable is said to be <em>unbound</em> in the environment.' },

  { t: 'sub', n: '3.2.1', title: 'The Rules for Evaluation' },
  { t: 'p', html: 'The overall specification of how the interpreter evaluates a combination remains the same as when we first introduced it in section 1.1.3: to evaluate a combination, evaluate the subexpressions and then apply the value of the operator subexpression to the values of the operand subexpressions. The environment model of evaluation replaces the substitution model in specifying what it means to apply a compound procedure to arguments.' },
  { t: 'p', html: 'In the environment model of evaluation, a procedure is always a pair consisting of some code and a pointer to an environment. Procedures are created in only one way: by evaluating a <code>lambda</code> expression. This produces a procedure whose code is obtained from the text of the <code>lambda</code> expression and whose environment is the environment in which the <code>lambda</code> expression was evaluated to produce the procedure.' },
  { t: 'p', html: 'Now we can describe how procedures are applied. To apply a procedure to arguments, create a new environment containing a frame that binds the parameters to the values of the arguments. The enclosing environment of this frame is the environment specified by the procedure. Now, within this new environment, evaluate the procedure body.' },
  { t: 'p', html: 'For example, consider the procedure definition <code>(define (square x) (* x x))</code> evaluated in the global environment. The procedure definition syntax is just syntactic sugar for an underlying implicit <code>lambda</code> expression: <code>(define square (lambda (x) (* x x)))</code>. This creates a procedure object and binds <code>square</code> to it in the global frame. The procedure object is a pair whose code is <code>(lambda (x) (* x x))</code> and whose environment is the global environment.' },
  { t: 'p', html: 'To evaluate <code>(square 5)</code>, we create a new environment E1 with a frame binding <code>x</code> to 5, whose enclosing environment is the global environment (the environment part of the <code>square</code> procedure). Within E1, we evaluate <code>(* x x)</code>, which yields 25.' },

  { t: 'sub', n: '3.2.2', title: 'Applying Simple Procedures' },
  { t: 'p', html: 'When we introduced the substitution model in section 1.1.5 we showed how the combination <code>(f 5)</code> evaluates to 136, given the following procedure definitions:' },
  { t: 'code', code: '(define (square x) (* x x))\n(define (sum-of-squares x y) (+ (square x) (square y)))\n(define (f a) (sum-of-squares (+ a 1) (* a 2)))' },
  { t: 'p', html: 'We can analyze the same example using the environment model. Evaluating <code>(f 5)</code> creates a new frame E1 where <code>a</code> is bound to 5. The body of <code>f</code> is <code>(sum-of-squares (+ a 1) (* a 2))</code>, which evaluates to <code>(sum-of-squares 6 10)</code>. This creates a new frame E2 where <code>x</code> is 6 and <code>y</code> is 10. Within E2, <code>(+ (square x) (square y))</code> calls <code>square</code> twice, each time creating a new frame (E3 and E4) binding <code>x</code> to 6 and then 10 respectively. The final result is 136.' },
  { t: 'p', html: 'The crucial point to observe is that each call to <code>square</code> creates a new environment containing a frame with a binding of <code>x</code>. The different frames keep the different values of <code>x</code> separate. Each environment created by <code>square</code> points back to the global environment, since that is the environment indicated by the <code>square</code> procedure object.' },

  { t: 'sub', n: '3.2.3', title: 'Frames as the Repository of Local State' },
  { t: 'p', html: 'We can turn to the environment model to see how procedures and assignment can be used to represent objects with local state. As an example, consider the &ldquo;withdrawal processor&rdquo; from section 3.1.1 created by calling <code>(define W1 (make-withdraw 100))</code>:' },
  { t: 'code', code: '(define (make-withdraw balance)\n  (lambda (amount)\n    (if (>= balance amount)\n        (begin (set! balance (- balance amount))\n               balance)\n        "Insufficient funds")))' },
  { t: 'p', html: 'When we define <code>make-withdraw</code> and call <code>(define W1 (make-withdraw 100))</code>, a frame E1 is created with <code>balance</code> bound to 100. The <code>lambda</code> body creates a procedure object whose environment is E1. When we later call <code>(W1 50)</code>, a new frame E2 is created binding <code>amount</code> to 50, with E1 as its enclosing environment. The body of <code>W1</code> executes <code>(set! balance (- balance amount))</code>, which changes the binding of <code>balance</code> in E1 to 50.' },
  { t: 'p', html: 'The key insight: the frame E1 persists because the procedure object <code>W1</code> points to it. Each call to <code>W1</code> creates a new frame that extends E1, and the <code>set!</code> modifies the binding in E1. This is how local state is maintained: the environment serves as the &ldquo;place&rdquo; that holds the state.' },
  { t: 'p', html: 'If we create a second withdrawal processor <code>(define W2 (make-withdraw 100))</code>, it creates its own frame E3 with its own <code>balance</code> binding. <code>W1</code> and <code>W2</code> have distinct local state because they point to different frames.' },

  { t: 'sub', n: '3.2.4', title: 'Internal Definitions' },
  { t: 'p', html: 'Section 1.1.8 introduced the idea that procedures can have internal definitions, thus leading to a block structure. Now we can understand internal definitions using the environment model. Consider a procedure with internal definitions:' },
  { t: 'code', code: '(define (sqrt x)\n  (define (good-enough? guess)\n    (< (abs (- (square guess) x)) 0.001))\n  (define (improve guess)\n    (average guess (/ x guess)))\n  (define (sqrt-iter guess)\n    (if (good-enough? guess)\n        guess\n        (sqrt-iter (improve guess))))\n  (sqrt-iter 1.0))' },
  { t: 'p', html: 'When <code>sqrt</code> is called, a new frame E1 is created binding <code>x</code>. The internal <code>define</code> forms add bindings for <code>good-enough?</code>, <code>improve</code>, and <code>sqrt-iter</code> to this same frame E1. The procedure objects created by these definitions all have E1 as their environment. This means they can all access <code>x</code> (and each other) without <code>x</code> being passed as an explicit parameter. The environment model thus explains how lexical scoping works: the local procedures are defined in the scope of <code>x</code>, so they &ldquo;see&rdquo; <code>x</code> in their enclosing environment.' },

  // ── 3.3 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '3.3', title: 'Modeling with Mutable Data' },
  { t: 'p', html: 'Chapter 2 dealt with compound data as a means for constructing computational objects that have several parts, in order to model real-world objects that have several aspects. In that chapter we introduced the discipline of data abstraction, according to which data structures are specified in terms of constructors, which create data objects, and selectors, which access the parts of compound data objects. But now we also need <em>mutators</em>, which modify data objects.' },
  { t: 'p', html: 'Data objects for which mutators are defined are known as <em>mutable data objects</em>. Chapter 2 introduced pairs as a general-purpose &ldquo;glue&rdquo; for synthesizing compound data. We now define mutators for pairs, called <code>set-car!</code> and <code>set-cdr!</code>, that enable us to use pairs to construct data structures that cannot be built with <code>cons</code>, <code>car</code>, and <code>cdr</code> alone.' },

  { t: 'sub', n: '3.3.1', title: 'Mutable List Structure' },
  { t: 'p', html: 'The primitive mutators for pairs are <code>set-car!</code> and <code>set-cdr!</code>. <code>Set-car!</code> takes two arguments, the first of which must be a pair. It modifies this pair, replacing the <code>car</code> pointer by a pointer to the second argument:' },
  { t: 'code', live: true, code: '(define x (list \'a \'b))\n(define z1 (cons x x))\n(define z2 (cons (list \'a \'b) (list \'a \'b)))\n\n(set-car! (cdr x) \'c)\nx\n(car z1)\n(cdr z1)\n(car z2)\n(cdr z2)' },
  { t: 'p', html: 'Notice the key difference: <code>z1</code> shares structure (both its <code>car</code> and <code>cdr</code> point to the same pair <code>x</code>), so mutating <code>x</code> is visible through both. <code>z2</code> does not share structure, so its <code>car</code> and <code>cdr</code> are independent. The predicate <code>eq?</code> tests whether two symbols refer to the same object (pointer equality), and this is how we detect sharing.' },
  { t: 'p', html: 'Mutation also allows us to represent queues and tables as mutable data structures.' },

  { t: 'subsub', title: 'Queues' },
  { t: 'p', html: 'A <em>queue</em> is a sequence in which items are inserted at one end (the rear) and deleted from the other end (the front). It is an example of a data structure whose efficient implementation requires mutation. By maintaining a pointer to both the front and rear of the sequence, we can implement all queue operations in O(1) time:' },
  { t: 'code', live: true, code: '(define (make-queue) (cons \'() \'()))\n(define (front-ptr queue) (car queue))\n(define (rear-ptr queue) (cdr queue))\n(define (set-front-ptr! queue item) (set-car! queue item))\n(define (set-rear-ptr! queue item) (set-cdr! queue item))\n(define (empty-queue? queue) (null? (front-ptr queue)))\n(define (front-queue queue)\n  (if (empty-queue? queue)\n      (error "FRONT called with an empty queue" queue)\n      (car (front-ptr queue))))\n(define (insert-queue! queue item)\n  (let ((new-pair (cons item \'())))\n    (cond ((empty-queue? queue)\n           (set-front-ptr! queue new-pair)\n           (set-rear-ptr! queue new-pair)\n           queue)\n          (else\n           (set-cdr! (rear-ptr queue) new-pair)\n           (set-rear-ptr! queue new-pair)\n           queue))))\n(define (delete-queue! queue)\n  (cond ((empty-queue? queue)\n         (error "DELETE! called with an empty queue" queue))\n        (else\n         (set-front-ptr! queue (cdr (front-ptr queue)))\n         queue)))\n\n(define q (make-queue))\n(insert-queue! q \'a)\n(insert-queue! q \'b)\n(front-queue q)\n(delete-queue! q)\n(front-queue q)' },

  { t: 'sub', n: '3.3.2', title: 'Representing Tables' },
  { t: 'p', html: 'A table is a collection of records, each of which is a key-value pair. We implement a table as a headed list: a list whose first element is a special &ldquo;header&rdquo; symbol. Records are stored as the rest of the list. We search for a record by scanning the list:' },
  { t: 'code', live: true, code: '(define (make-table) (list \'*table*))\n(define (lookup key table)\n  (let ((record (assoc key (cdr table))))\n    (if record (cdr record) #f)))\n(define (assoc key records)\n  (cond ((null? records) #f)\n        ((equal? key (caar records)) (car records))\n        (else (assoc key (cdr records)))))\n(define (insert! key value table)\n  (let ((record (assoc key (cdr table))))\n    (if record\n        (set-cdr! record value)\n        (set-cdr! table\n                  (cons (cons key value) (cdr table)))))\n  \'ok)\n\n(define t (make-table))\n(insert! \'x 1 t)\n(insert! \'y 2 t)\n(lookup \'x t)\n(lookup \'y t)\n(lookup \'z t)' },
  { t: 'p', html: 'This can be extended to two-dimensional tables, where each key maps to a subtable. Such tables are used to implement the operation-and-type table for data-directed programming (section 2.4.3).' },

  { t: 'sub', n: '3.3.3', title: 'Representing Queues' },
  { t: 'p', html: 'The queue implementation shown above demonstrates a general principle: by giving the data structure a &ldquo;header&rdquo; (a fixed pair whose <code>car</code> and <code>cdr</code> are mutated), we can write mutation procedures that modify the queue without needing to return it. The header acts as the queue&rsquo;s identity.' },
  { t: 'p', html: 'This same technique extends to other mutable data structures. A particularly useful one is the <em>deque</em> (double-ended queue), where insertion and deletion can happen at both the front and rear.' },

  { t: 'sub', n: '3.3.4', title: 'A Simulator for Digital Circuits' },
  { t: 'p', html: 'To illustrate the power of mutable data, we design a system for simulating digital logic circuits. This example also illustrates the &ldquo;event-driven&rdquo; style of programming, in which actions are scheduled to occur at particular times.' },
  { t: 'p', html: 'Primitive function boxes implement the logical operations: an inverter, an and-gate, and an or-gate. Each has input wires and output wires. Connecting primitive function boxes forms larger circuits. For example, a half-adder can be constructed from two and-gates, an or-gate, and an inverter:' },
  { t: 'code', code: '(define (half-adder a b s c)\n  (let ((d (make-wire)) (e (make-wire)))\n    (or-gate a b d)\n    (and-gate a b c)\n    (inverter c e)\n    (and-gate d e s)\n    \'ok))\n\n(define (full-adder a b c-in sum c-out)\n  (let ((s (make-wire)) (c1 (make-wire)) (c2 (make-wire)))\n    (half-adder b c-in s c1)\n    (half-adder a s sum c2)\n    (or-gate c1 c2 c-out)\n    \'ok))' },
  { t: 'p', html: 'The key abstraction is the <em>wire</em>. A wire holds a signal value and maintains a list of <em>action procedures</em> that should be run whenever the signal changes. This is done via <code>add-action!</code>. When a gate&rsquo;s input changes, it schedules its output change after an appropriate delay using the <em>agenda</em>, a time-ordered queue of things to do.' },
  { t: 'code', code: '(define (inverter input output)\n  (define (invert-input)\n    (let ((new-value (logical-not (get-signal input))))\n      (after-delay inverter-delay\n                   (lambda () (set-signal! output new-value)))))\n  (add-action! input invert-input)\n  \'ok)\n\n(define (and-gate a1 a2 output)\n  (define (and-action-procedure)\n    (let ((new-value\n           (logical-and (get-signal a1) (get-signal a2))))\n      (after-delay and-gate-delay\n                   (lambda () (set-signal! output new-value)))))\n  (add-action! a1 and-action-procedure)\n  (add-action! a2 and-action-procedure)\n  \'ok)' },
  { t: 'p', html: 'The agenda data structure and the simulation loop (<code>propagate</code>) drive the simulation forward by executing scheduled actions in time order. The entire simulator is built from just mutable pairs, local state, and higher-order procedures.' },

  { t: 'sub', n: '3.3.5', title: 'Propagation of Constraints' },
  { t: 'p', html: 'Computer programs are traditionally organized as one-directional computations, which perform operations on prespecified arguments to produce desired outputs. On the other hand, we often model systems in terms of relations among quantities. A constraint system allows us to express relations and have the system propagate information in any direction.' },
  { t: 'p', html: 'For example, the relationship between Fahrenheit and Celsius temperatures is 9C = 5(F &minus; 32). A constraint network for this relationship uses adder, multiplier, and constant constraints connected by connectors:' },
  { t: 'code', code: '(define (celsius-fahrenheit-converter c f)\n  (let ((u (make-connector))\n        (v (make-connector))\n        (w (make-connector))\n        (x (make-connector))\n        (y (make-connector)))\n    (multiplier c w u)\n    (multiplier v x u)\n    (adder v y f)\n    (constant 9 w)\n    (constant 5 x)\n    (constant 32 y)\n    \'ok))' },
  { t: 'p', html: 'Given this network, if we set C = 25, the system propagates through the constraints to compute F = 77. Conversely, if we set F = 212, it computes C = 100. The constraint system works bidirectionally: information flows from any known connector through the constraints to determine unknown values. This is a fundamentally different model of computation from the one-directional view. Connectors hold values and notify constraints when they change; constraints check whether they have enough information to determine a new value and, if so, set the appropriate connector. This style of programming is called <em>constraint propagation</em>.' },

  // ── 3.4 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '3.4', title: 'Concurrency: Time Is of the Essence' },
  { t: 'p', html: 'We have seen the power of computational objects with local state as a tool for modeling. Yet this power extracts a price: the loss of referential transparency, giving rise to a thicket of questions about sameness and change, and the need to abandon the substitution model of evaluation in favor of the more intricate environment model. The central issue lurking beneath the complexity of state, sameness, and change is that by introducing assignment we are forced to admit <em>time</em> into our computational models.' },
  { t: 'p', html: 'The complexities introduced by assignment become even more problematic in the presence of concurrency. Suppose that Peter and Paul share a joint bank account with a balance of $100. Peter deposits $10 and Paul withdraws $25. If these operations happen sequentially, the final balance is $85 regardless of order. But if they happen concurrently, the results depend on the interleaving of the individual operations within each transaction.' },

  { t: 'sub', n: '3.4.1', title: 'The Nature of Time in Concurrent Systems' },
  { t: 'p', html: 'Suppose Peter and Paul each access the balance (100), then Peter adds 10 (setting it to 110) and Paul subtracts 25 (setting it to 75, based on his stale read of 100). The balance is now 75 instead of the correct 85. The problem is that Peter&rsquo;s deposit was &ldquo;lost&rdquo; because Paul&rsquo;s operation was based on a value that was subsequently changed.' },
  { t: 'p', html: 'The root of this complexity lies in the assignments to variables shared among concurrent processes. One possible restriction is to require that concurrent processes not share state. But this is too restrictive for modeling systems that interact. A less stringent restriction is to ensure that concurrent processes produce results as if they ran sequentially in some order. This is called <em>serialization</em>.' },

  { t: 'sub', n: '3.4.2', title: 'Mechanisms for Controlling Concurrency' },
  { t: 'p', html: 'The serializer concept groups procedures so that only one procedure in each serializer&rsquo;s group can execute at a time. For the bank account:' },
  { t: 'code', code: '(define (make-account balance)\n  (define (withdraw amount)\n    (if (>= balance amount)\n        (begin (set! balance (- balance amount))\n               balance)\n        "Insufficient funds"))\n  (define (deposit amount)\n    (set! balance (+ balance amount))\n    balance)\n  (let ((protected (make-serializer)))\n    (define (dispatch m)\n      (cond ((eq? m \'withdraw) (protected withdraw))\n            ((eq? m \'deposit) (protected deposit))\n            ((eq? m \'balance) balance)\n            (else (error "Unknown request" m))))\n    dispatch))' },
  { t: 'p', html: 'With the serializer, each deposit or withdrawal runs atomically: no interleaving of shared-state accesses is possible. The serializer can be implemented using a <em>mutex</em> (mutual exclusion), a synchronization mechanism that can be <em>acquired</em> and <em>released</em>. A serializer produces a wrapped version of a procedure that acquires the mutex before running the original procedure and releases it afterward.' },
  { t: 'p', html: 'Serializers create the danger of <em>deadlock</em>: if process A holds serializer 1 and waits for serializer 2, while process B holds serializer 2 and waits for serializer 1, neither can proceed. Deadlock avoidance is a significant challenge in concurrent systems design.' },

  // ── 3.5 ──────────────────────────────────────────────────────────────
  { t: 'sec', n: '3.5', title: 'Streams' },
  { t: 'p', html: 'We have seen how assignment and mutable objects can model systems with state. But there is an alternative approach. We can model change in terms of sequences that represent the time histories of the systems being modeled. A &ldquo;withdrawal processor&rdquo; is modeled not as an object with changing state, but as a procedure that maps a stream of input requests to a stream of output balances. With this approach, we achieve modularity without side effects.' },

  { t: 'sub', n: '3.5.1', title: 'Streams Are Delayed Lists' },
  { t: 'p', html: 'On the surface, streams are just lists. But streams use <em>delayed evaluation</em> to avoid computing elements until they are needed. The key idea is that <code>cons-stream</code> does not evaluate its second argument until it is accessed by <code>stream-cdr</code>. This allows us to work with infinitely long sequences.' },
  { t: 'p', html: '<code>cons-stream</code> is a special form such that <code>(cons-stream a b)</code> is equivalent to <code>(cons a (delay b))</code>. The expression <code>b</code> is not evaluated until <code>(force (cdr ...))</code> is called. <code>Delay</code> and <code>force</code> can be implemented using procedures: <code>(delay exp)</code> is <code>(lambda () exp)</code>, and <code>(force promise)</code> is <code>(promise)</code>. In practice, <code>delay</code> also memoizes the result so the computation happens at most once.' },
  { t: 'code', live: true, code: '(define (stream-ref s n)\n  (if (= n 0)\n      (stream-car s)\n      (stream-ref (stream-cdr s) (- n 1))))\n\n(define (stream-enumerate-interval low high)\n  (if (> low high)\n      the-empty-stream\n      (cons-stream low\n                   (stream-enumerate-interval (+ low 1) high))))\n\n(define s (stream-enumerate-interval 1 20))\n(stream-ref s 0)\n(stream-ref s 5)\n(stream-ref s 19)' },

  { t: 'sub', n: '3.5.2', title: 'Infinite Streams' },
  { t: 'p', html: 'With delayed evaluation, we can define streams that are infinitely long. The elements are computed only as needed. For example, the stream of all positive integers:' },
  { t: 'code', live: true, code: '(define (integers-starting-from n)\n  (cons-stream n (integers-starting-from (+ n 1))))\n(define integers (integers-starting-from 1))\n(stream-ref integers 0)\n(stream-ref integers 10)\n(stream-ref integers 99)' },
  { t: 'p', html: 'We can define more interesting infinite streams. The Sieve of Eratosthenes produces the stream of all primes:' },
  { t: 'code', live: true, code: '(define (divisible? x y) (= (remainder x y) 0))\n(define (sieve stream)\n  (cons-stream\n   (stream-car stream)\n   (sieve (stream-filter\n           (lambda (x) (not (divisible? x (stream-car stream))))\n           (stream-cdr stream)))))\n(define primes (sieve (integers-starting-from 2)))\n(stream-ref primes 0)\n(stream-ref primes 10)\n(stream-ref primes 49)' },
  { t: 'p', html: 'Another approach to defining infinite streams is through <em>implicit definitions</em>. We can define streams in terms of themselves:' },
  { t: 'code', live: true, code: '(define ones (cons-stream 1 ones))\n(define (add-streams s1 s2)\n  (stream-map + s1 s2))\n(define integers (cons-stream 1 (add-streams ones integers)))\n(stream-ref integers 0)\n(stream-ref integers 5)\n(stream-ref integers 99)' },
  { t: 'p', html: 'The Fibonacci numbers can also be defined implicitly:' },
  { t: 'code', live: true, code: '(define ones (cons-stream 1 ones))\n(define (add-streams s1 s2) (stream-map + s1 s2))\n(define fibs\n  (cons-stream 0\n               (cons-stream 1\n                            (add-streams (stream-cdr fibs) fibs))))\n(stream-ref fibs 0)\n(stream-ref fibs 1)\n(stream-ref fibs 10)\n(stream-ref fibs 20)' },

  { t: 'sub', n: '3.5.3', title: 'Exploiting the Stream Paradigm' },
  { t: 'p', html: 'Streams with delayed evaluation can be a powerful modeling tool, providing many of the benefits of local state and assignment while avoiding some of the theoretical tangles. We can reformulate the square-root computation from section 1.1.7 as a stream computation:' },
  { t: 'code', live: true, code: '(define ones (cons-stream 1 ones))\n(define (add-streams s1 s2) (stream-map + s1 s2))\n(define (scale-stream stream factor)\n  (stream-map (lambda (x) (* x factor)) stream))\n\n(define (sqrt-improve guess x)\n  (/ (+ guess (/ x guess)) 2.0))\n(define (sqrt-stream x)\n  (define guesses\n    (cons-stream 1.0\n                 (stream-map (lambda (guess) (sqrt-improve guess x))\n                             guesses)))\n  guesses)\n(stream-ref (sqrt-stream 2) 0)\n(stream-ref (sqrt-stream 2) 1)\n(stream-ref (sqrt-stream 2) 5)' },
  { t: 'p', html: 'The stream formulation has a satisfying elegance: we generate the entire infinite sequence of improving guesses, and simply reach in for the one we need. The computation is demand-driven: only as many elements as we request are actually computed.' },
  { t: 'p', html: 'We can also generate approximations to &pi; using an infinite series. The Leibniz formula states that &pi;/4 = 1 &minus; 1/3 + 1/5 &minus; 1/7 + &hellip;' },
  { t: 'code', live: true, code: '(define (partial-sums s)\n  (define sums\n    (cons-stream (stream-car s)\n                 (add-streams (stream-cdr s) sums)))\n  sums)\n(define ones (cons-stream 1 ones))\n(define (add-streams s1 s2) (stream-map + s1 s2))\n(define (pi-summands n)\n  (cons-stream (/ 1.0 n)\n               (stream-map - (pi-summands (+ n 2)))))\n(define pi-stream\n  (scale-stream (partial-sums (pi-summands 1)) 4))\n(define (scale-stream s f) (stream-map (lambda (x) (* x f)) s))\n(stream-ref pi-stream 0)\n(stream-ref pi-stream 10)\n(stream-ref pi-stream 100)' },

  { t: 'sub', n: '3.5.4', title: 'Streams and Delayed Evaluation' },
  { t: 'p', html: 'The use of <code>delay</code> in <code>cons-stream</code> is crucial for the stream paradigm to work. Without it, attempting to define an infinite stream would cause an infinite loop. The <code>delay</code> decouples the time of an expression&rsquo;s appearance from the time of its evaluation.' },
  { t: 'p', html: 'However, there are cases where <code>cons-stream</code>&rsquo;s implicit delay is not sufficient. Consider modeling a signal-processing system with feedback. The output stream depends on the input stream, but the input stream may itself depend on the output. Such circular definitions require explicit use of <code>delay</code> in procedure arguments; a procedure that takes a delayed argument and forces it internally. This is called <em>normal-order evaluation</em>, and it is the topic we explore with the lazy evaluator in section 4.2.' },

  { t: 'sub', n: '3.5.5', title: 'Modularity of Functional Programs and Modularity of Objects' },
  { t: 'p', html: 'One of the major benefits of introducing assignment was the ability to modularly construct programs that model systems with state (section 3.1.2). The stream approach achieves the same modularity without assignment. For example, consider a Monte Carlo estimation of &pi;. The imperative version uses <code>rand</code> with internal state and an accumulator. The stream version generates an infinite stream of random numbers, maps the experiment over it, and computes running estimates:' },
  { t: 'code', code: '(define (monte-carlo experiment-stream passed failed)\n  (define (next passed failed)\n    (cons-stream\n     (/ passed (+ passed failed))\n     (monte-carlo\n      (stream-cdr experiment-stream) passed failed)))\n  (if (stream-car experiment-stream)\n      (next (+ passed 1) failed)\n      (next passed (+ failed 1))))' },
  { t: 'p', html: 'The stream approach factors the program differently: the random-number generator, the experiment, and the accumulation of results are all separate stream operations, composed through stream operations rather than entangled through shared state.' },
  { t: 'p', html: 'However, the stream approach has its own difficulties. When we model the interaction of independent stateful objects (like separate bank accounts), the merge of their individual event streams reintroduces the need to deal with time explicitly. Neither the object approach (with assignment) nor the stream approach (with lazy evaluation) is clearly superior in all cases. The tension between these two worldviews is one of the deep themes of this book.' },
];
